Name: 🎙️ Podcast Generator - Full Pipeline (Final Fix)

on:
  schedule:
    # Chạy 3 lần/ngày vào các khung giờ vàng đã tối ưu chiến lược (UTC):
    # 11:40 (18:40 VN), 14:40 (09:40 Sáng Mỹ), 21:40 (04:40 Chiều Mỹ)
    # Lịch này giúp bảo vệ Quota API và GitHub Minutes.
    - cron: '40 11,14,21 * * *'
  
  workflow_dispatch: # Cho phép chạy thủ công

jobs:
  build_and_upload_podcast:
    runs-on: ubuntu-latest

    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
      SERVICE_ACCOUNT_CONTENT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON_CONTENT }} 
      YOUTUBE_CLIENT_SECRET_B64: ${{ secrets.YOUTUBE_CLIENT_SECRET_B64 }}
      YOUTUBE_TOKEN_PICKLE_B64: ${{ secrets.YOUTUBE_TOKEN_PICKLE_B64 }}

    steps:
      - name: ⬇️ Checkout mã nguồn
        uses: actions/checkout@v4

      # --- [TỐI ƯU HÓA] CACHING PYTHON ---
      - name: 🐍 Thiết lập môi trường Python (Có Cache)
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          # [CHIẾN LƯỢC TỐI ƯU] 
          # Dòng 'cache: pip' kích hoạt bộ nhớ đệm. 
          # Nó giúp bỏ qua việc tải lại các thư viện nặng (numpy, moviepy...) nếu file requirements.txt không đổi.
          # Tiết kiệm 2-5 phút mỗi lần chạy.
          cache: 'pip' 
          cache-dependency-path: 'requirements.txt'

      # --- Cài đặt System Dependencies (FFmpeg + ImageMagick) ---
      # Lưu ý: FFmpeg cài bằng apt-get khá nhanh nên không cần cache phức tạp.
      - name: ⚙️ Cài đặt FFmpeg & ImageMagick
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg imagemagick
          # QUAN TRỌNG: Sửa lỗi bảo mật của ImageMagick để cho phép tạo TextClip
          sudo sed -i 's/none/read,write/g' /etc/ImageMagick-6/policy.xml

      - name: 📦 Cài đặt các thư viện Python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        
      # --- Giải mã Khóa ---
      - name: 🔑 Giải mã Khóa Xác thực
        run: |
          echo "$SERVICE_ACCOUNT_CONTENT" > service-account.json
          echo "${{ secrets.YOUTUBE_CLIENT_SECRET_B64 }}" | base64 --decode > client_secrets.json
          echo "${{ secrets.YOUTUBE_TOKEN_PICKLE_B64 }}" | base64 --decode > token.pickle
          
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" > .env
          echo "GOOGLE_SHEET_ID=${{ secrets.GOOGLE_SHEET_ID }}" >> .env
          echo "GOOGLE_SERVICE_ACCOUNT_JSON=service-account.json" >> .env

      # --- Chạy Pipeline ---
      - name: 🚀 Chạy Podcast Generator Pipeline
        run: python scripts/glue_pipeline.py

      # 💾 --- Lưu trữ Output (Artifact) ---
      - name: 💾 Lưu trữ TOÀN BỘ Output (Artifacts)
        uses: actions/upload-artifact@v4
        if: always() # Đảm bảo chạy ngay cả khi Pipeline thất bại để debug
        with:
          name: episode-output-artifacts
          path: outputs/ 
          retention-days: 7 
